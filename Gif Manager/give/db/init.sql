CREATE TABLE IF NOT EXISTS secret (
    id SERIAL PRIMARY KEY,
    flag TEXT NOT NULL DEFAULT 'write admin if you see it'
);

INSERT INTO secret (flag) VALUES ('stctf{??????????????????????????????}');